# development | staging | production (defaults to "development"; EAS build profiles set this automatically)
APP_ENV=development

# Overrides the environment's default backend API base URL
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080
