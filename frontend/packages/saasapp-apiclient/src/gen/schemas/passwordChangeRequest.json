{
    "type": "object",
    "properties": {
        "currentPassword": { "type": "string", "minLength": 1 },
        "newPassword": {
            "type": "string",
            "maxLength": 2147483647,
            "minLength": 8,
            "pattern": "^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#?!@$ %^&*-]).{8,}$"
        }
    },
    "required": ["currentPassword", "newPassword"],
    "x-readme-ref-name": "PasswordChangeRequest"
}
